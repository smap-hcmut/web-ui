apiVersion: v1
kind: Secret
metadata:
  name: smap-web-secret
  namespace: smap
  labels:
    app: smap-web
    component: frontend
type: Opaque
stringData:
  # Add your sensitive environment variables here if needed
  # Example:
  # API_KEY: "your-api-key"
  # DATABASE_URL: "your-database-url"
  # SECRET_KEY: "your-secret-key"

  # Placeholder - remove this line when you add real secrets
  PLACEHOLDER: "no-secrets-needed-yet"
